CREATE TABLE PLAYING_FIELDS (
    _id INTEGER PRIMARY KEY NOT NULL UNIQUE,
    playingFieldId TEXT NOT NULL UNIQUE,
    name TEXT NOT NULL,
    latitude REAL,
    longitude REAL,
);

CREATE INDEX UNIQUE_NAME ON PLAYING_FIELDS (name);